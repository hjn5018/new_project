version: '3.8'

services:
  redis:
    image: redis:7.2-alpine
    container_name: poc1-redis
    ports:
      - "6379:6379"
    command: redis-server --maxmemory 256mb --maxmemory-policy volatile-lru
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
